# Client-safe public config only. Never put Stripe, Shopify, or Supabase service keys here.
# Copy to a local .env if you need overrides. Preview/production injects DATABASE_URL separately.

VITE_STORE_BASE_URL=
VITE_PRODUCT_CATALOG_URL=/config/store-products.json
VITE_ALLOWED_PARENT_ORIGIN=
VITE_ANALYTICS_ENABLED=true
VITE_GAME_BUILD_VERSION=0.9.0-rc
